# Production Environment Variables Template
# Copy this to your deployment platform's environment variable settings

# ============================================
# RAILWAY (Backend) Environment Variables
# ============================================
GEMINI_API_KEY=your_actual_gemini_api_key_here
PORT=3001
FRONTEND_URL=https://your-app.vercel.app
NODE_ENV=production

# ============================================
# VERCEL (Frontend) Environment Variables
# ============================================
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here
VITE_API_URL=https://your-backend.up.railway.app/api

# ============================================
# IMPORTANT NOTES
# ============================================
# 1. Never commit actual values to git
# 2. GEMINI_API_KEY must NOT have VITE_ prefix (server-side only)
# 3. VITE_ prefixed variables are exposed to browser (client-side)
# 4. Update FRONTEND_URL after deploying to Vercel
# 5. Update VITE_API_URL after deploying to Railway
